FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --production

COPY lab-api ./lab-api

ENV LAB_API_HOST=0.0.0.0
ENV LAB_API_PORT=4000

EXPOSE 4000

CMD ["node", "lab-api/server.js"]
